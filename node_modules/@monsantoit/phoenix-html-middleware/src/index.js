const generateHtmlMiddleware = require('./generateHtmlMiddleware')
const getSuiteStylesheetUrl = require('./getSuiteStylesheetUrl')
const getSuiteForHost = require('./getSuiteForHost')

module.exports = {
    generateHtmlMiddleware,
    getSuiteStylesheetUrl,
    getSuiteForHost,
}
