"use strict";

var browser = require('./browser');

var _ = require('../lodash');

module.exports = {
  set: function set(key, value) {
    _.set(browser.namespace(), key, value);

    return value;
  },
  get: function get(key, defaultValue) {
    return _.get(browser.namespace(), key, defaultValue);
  }
};