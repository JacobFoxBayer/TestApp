// eslint-disable-next-line import/extensions
import themesArray from './themesArray';

const buildThemesMenu = () => {
    return themesArray.map(theme => {
        return { value: theme.slug, title: theme.label };
    });
};

export default buildThemesMenu;
