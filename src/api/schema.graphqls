type Dog {
  dogId: Int
  name: String
  age: Int
  breed: String
}

input DogInput {
  name: String
  age: Int
  breed: String
}

type Query {
  getData: [Dog]
}

type Mutation {
  changeData(input: DogInput): [Dog]
}
